Claude, necesito crear el módulo de Registro de Arribos.

La URL es /dashboard/arribos/nuevo (que corresponde a app/(dashboard)/dashboard/arribos/nuevo/page.tsx)

Crea los siguientes archivos:

1. **app/(dashboard)/dashboard/arribos/nuevo/page.tsx** - Formulario de registro

El formulario debe tener estos campos:
- Tienda (Select) - Lista de tiendas del usuario
- DNI Cliente (Input texto, opcional) - 8 dígitos
- ¿Es cliente Entel? (Select: Sí/No/No sabe)
- Tipo de visita (Select: Venta/Posventa)
- ¿Concretó operación? (Select: Sí/No)
- ¿Se vendió? (Select: Sí/No) - Solo visible si Tipo visita = Venta
- Motivo no venta (Select) - Solo visible si Se vendió = No

Motivos de no venta:
- Sin stock
- Precio muy alto
- No califica crédito
- Solo consulta
- Documentos incompletos
- Problema sistema
- Otro

Funcionalidad:
- Usar 'use client'
- Obtener usuario del localStorage
- Cargar tiendas del usuario desde Supabase (tabla tiendas)
- Si el usuario es ADMIN, cargar todas las tiendas
- Si no, cargar solo las tiendas asignadas (tabla usuarios_tiendas)
- Al guardar, insertar en tabla "arribos" con:
  - fecha: fecha actual
  - hora: hora actual
  - tienda_id: tienda seleccionada
  - usuario_id: id del usuario logueado
  - registrado_por: id del usuario logueado
  - dni_cliente: valor ingresado (puede ser null)
  - es_cliente_entel: boolean
  - tipo_visita: 'VENTA' o 'POSVENTA'
  - concreto_operacion: boolean
  - se_vendio: boolean (null si tipo_visita = POSVENTA)
  - motivo_no_venta: string (null si se_vendio = true)
- Mostrar mensaje de éxito y limpiar formulario
- Usar componentes de shadcn/ui (Card, Form, Select, Input, Button)
- Usar react-hook-form con zod para validación

2. **app/api/arribos/route.ts** - API para guardar arribos
```typescript
import { NextRequest, NextResponse } from 'next/server'
import { createClient } from '@/lib/supabase/server'

export async function POST(request: NextRequest) {
  try {
    const body = await request.json()
    const supabase = await createClient()
    
    const { data, error } = await supabase
      .from('arribos')
      .insert(body)
      .select()
      .single()
    
    if (error) {
      return NextResponse.json({ error: error.message }, { status: 400 })
    }
    
    return NextResponse.json({ success: true, arribo: data })
  } catch (error) {
    return NextResponse.json({ error: 'Error interno' }, { status: 500 })
  }
}
```

Crea estos archivos con la estructura correcta de Next.js App Router.